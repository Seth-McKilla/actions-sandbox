name: Workflow Dispatch

on:
  workflow_dispatch:

env:
  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

jobs:
  workflow-dispatch:
    runs-on: ubuntu-latest

    steps:
      - name: Sucess
        id: success
        run: echo "Success!"
